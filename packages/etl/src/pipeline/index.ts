/**
 * Public surface of the pipeline shell (Stage 1.1): the "add a source"
 * extension point (`source.ts`) and the orchestration that sequences it
 * with name resolution (`run.ts`).
 */

export * from './source.ts';
export * from './run.ts';

{
  "name": "@garden-planner/etl",
  "version": "0.0.0",
  "private": true,
  "description": "Build-time data pipeline (developer tool, NOT shipped). Ingests plant sources and emits the /data artifact.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "build:data": "node --experimental-strip-types src/build-data.ts",
    "start": "node --experimental-strip-types src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@garden-planner/engine": "*"
  }
}

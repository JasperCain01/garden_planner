{
  "name": "garden-planner",
  "version": "0.0.0",
  "private": true,
  "description": "Offline-capable, static-hosted planner for edible gardens and allotments.",
  "type": "module",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "app"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev": "npm run dev -w app",
    "preview": "npm run preview -w app",
    "e2e": "npm run e2e -w app",
    "verify": "npm run lint && npm run typecheck && npm run format:check && npm test && npm run build && npm run e2e",
    "deploy": "GITHUB_PAGES=true npm run build -w app && gh-pages -d app/dist",
    "smoke:deployed": "npm run smoke:deployed -w app"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/node": "^22.10.0",
    "eslint": "^9.15.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react-hooks": "^5.1.0",
    "gh-pages": "^6.3.0",
    "globals": "^15.12.0",
    "prettier": "^3.4.0",
    "svgo": "^4.0.2",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.16.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
